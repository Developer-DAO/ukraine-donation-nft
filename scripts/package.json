{
  "name": "scripts",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "postinstall": "npm run typechain && npm run build",
    "typechain": "rimraf src/types/contract && typechain --target ethers-v5 --out-dir src/types/contracts src/abis/*.json",
    "build": "rimraf lib && tsc",
    "transfer": "node lib/index.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "ethers": "^5.6.4"
  },
  "devDependencies": {
    "@typechain/ethers-v5": "^10.0.0",
    "@types/node": "^17.0.27",
    "rimraf": "^3.0.2",
    "ts-node": "^10.7.0",
    "typechain": "^8.0.0",
    "typescript": "^4.6.3"
  }
}
